create or replace table `pacc-raw-data.BASEVN_PAYROLL.cycle` (
    id string,
    user_id string,
    username string,
    schedule string,
    start_date string,
    end_date string,
    report_month string,
    report_year string,
    since string,
    last_update string,
    system_id string,
    loaded_date timestamp
)