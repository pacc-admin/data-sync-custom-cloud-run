create or replace table `pacc-raw-data.BASEVN_PAYROLL.record` (
      id string,
      user_id string,
      employee_id string,
      employee_code string,
      cycle_id string,
      cycle_export string,
      loaded_date timestamp
)